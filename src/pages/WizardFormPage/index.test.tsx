import { render } from "@testing-library/react";
import { WizardFormPage } from ".";

describe(" WizardForm testing", () => {
  it("render component", () => {
    render(<WizardFormPage />);
  });
});
